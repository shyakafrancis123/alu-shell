#!/usr/bin/env bash

# Get the PID of the process named 4-to_infinity_and_beyond
pid=$(pgrep -f 4-to_infinity_and_beyond)

# If PID exists, send SIGTERM using /proc indirect method
if [ -n "$pid" ]; then
  echo "Terminating $pid"
  printf '\x0f' > "/proc/$pid/fd/0"
fi

